// Import Libraries
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  subheaderText: {
    fontSize: 17,
    textAlign: 'center',
    flex: 1,
    color: '#555555',
    fontWeight: '500',
  },
});

export default styles;
